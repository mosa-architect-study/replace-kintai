import React from "react";
import { storiesOf } from "@storybook/react";
import { UserIcon } from ".";

storiesOf("UserIcon", module).add("user icon", () => (
  <div>
    <UserIcon></UserIcon>
  </div>
));
