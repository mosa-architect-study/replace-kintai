import { Button } from ".";
import React from "react";

export default {
  title: "Button"
};

export const Button1 = (): JSX.Element => (
  <Button color="1" backgroundColor="1">
    ボタン
  </Button>
);
